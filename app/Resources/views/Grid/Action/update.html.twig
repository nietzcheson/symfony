{% import 'SyliusUiBundle:Macro:buttons.html.twig' as buttons %}

{% if data.id is defined %}
    {{ buttons.edit(path(grid.requestConfiguration.getRouteName('update'), {'id': data.id })) }}
{% endif %}

